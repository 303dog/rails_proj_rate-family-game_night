<!DOCTYPE html>
    <html>
        
        <body>
            <div class="line">
                <p><u><h2>Game Library</h2><u></p>            
            </div>
            <div class="game__cards"><%@games.each do |gam|%>
            <div class="game__card">
            <div class="games__cards">
            <p><b><%=link_to gam.title, game_path(gam)%></b></p> 
            <%=gam.description%>
            </div>
          </div>
        <%end%>
            </div>
        
            <footer>
            <span class="line"></span>
                <p><%=link_to "Submit A Game", new_game_path%></p>
                <p><%=link_to "Game Reviews", reviews_path%></p>
            </footer>
        
        </body>
    </html>
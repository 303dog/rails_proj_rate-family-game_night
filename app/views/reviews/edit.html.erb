<!DOCTYPE html>
    <html>
        
        <h2>Edit this review of <u><%=@review.game.title%></u></h2>
    <div class="line"></div>
        <p><%=form_for (@review) do |f|%>
    <div class='edit-container'>
        <%= f.label :rating, "Rate the game 1-5"%><br>
        <%= f.number_field :rating%><br>
        <%= f.label :label, "Title" %><br>
        <%= f.text_field :label, :maxlength => 25 %><br><br>
        <%= f.label :summary, "Review"%><br>
        <%= f.text_area :summary, :maxlength => 600 %><br><br></p>
        <%= f.submit %>
        <%end%>
    </div>
    <div class="line"></div>
    
    <footer>
        <%=button_to "Destroy", review_path(@review), method: :delete, data: { confirm: "Are you sure you want to delete this review?"}%>

    </footer>

